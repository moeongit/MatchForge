#!/bin/sh

set -e

if ! command -v docker >/dev/null 2>&1; then
  echo "Docker is required to run ./mvnw in this repository."
  exit 1
fi

docker run --rm \
  -v "$(pwd)":/workspace \
  -w /workspace \
  maven:3.9.9-eclipse-temurin-21 \
  mvn "$@"
